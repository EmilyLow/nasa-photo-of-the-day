import React from "react";

const Title = props => {
    //console.log("Props from Title", props);

    return (
        <h1>
            {props.title}
        </h1>
            
    )
    


};

export default Title;